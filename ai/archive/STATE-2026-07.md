# STATE arşivi — 2026 Temmuz

> `ai/STATE.md` kısa kalsın diye taşınan detaylı geçmiş. Kararların gerekçesi
> `ai/DECISIONS.md`'de, dersler `ai/LESSONS.md`'de, inşa dönemi
> `docs/TESLIMAT-ARSIV.md`'de.

## 2026-07-07 → 07-21 — İnşa dönemi (Faz 1-7)
Sistem sıfırdan kuruldu ve GitHub Actions'ta otonom hale geldi. Ayrıntı:
`docs/TESLIMAT-ARSIV.md`.

- **2026-07-21** — z-skor kapısı düzeltmesi: gün sayıyor, kayıt değil (commit `71c7ba2`).
  Kayıt tabanlı kapıda sinyal 60 günlük dağılım yerine 13 günlük ortalamadan sapma
  ölçüyordu.

## 2026-07-24 — Denetim + altyapı günü

- **Çoklu-IDE Usta sistemi proje köküne kuruldu.** AGENTS.md kanonik; CLAUDE/GEMINI/
  copilot/Kiro köprüleri + `ai/` hafıza katmanı. Sistem daha önce `Proje Yardımcısı/`
  alt klasöründeydi ve hiçbir IDE onu okumadığı için **atıl duruyordu**
  (DECISIONS #002, LESSONS L-004).

- **[#1] Veri kapsaması ölçüldü + kaynak-retry eklendi.** Kapsama %62'nin sebebi
  GitHub Actions cron throttling'i (platform kısıtı, ücretsiz düzeltilemez); gerçek
  ritim medyan 94 dk. Asıl düzeltilebilir sorun %6.9 geçersiz kayıttı (truncgil
  geçici hataları) → `sources.fetch_retries` (DECISIONS #003).

- **[#2] Telegram export denetlendi.** 83 mesaj (22 Nis → 23 Tem): 21 alarm, 24 rapor.
  24s soğuma **0 ihlal**, günlük tavan aşımı yok (max 2/gün), hafta sonu bastırma
  doğru, rapor↔repo **%100** eşleşti. Bot sadık çalışıyor.

- **[#3] Giden mesaj arşivi (outbox).** `send_message` → `data/telegram_outbox.jsonl`;
  archive.yml commit'liyor. Artık denetim için Telegram export'una gerek yok.

- **İÇERİK denetimi — 17 günlük gerçek hata bulundu.** Export'taki 7 "hareket > 2×ATR"
  alarmının hepsi birebir aynı `ATR(75)` yazıyordu. Kök sebep: `history_daily` tablosu
  2026-07-07'de donmuştu — yalnız elle çalıştırılan bir backfill script'i besliyordu,
  otomatik pipeline'a bağlı değildi. ATR ve "günlük hareket" alarmları 17 gündür
  **yanlış referanstan** hesaplanıyordu. `history.update_recent()` yazılıp daily_job'a
  bağlandı; ATR 75(donuk) → 80.5(taze) (DECISIONS #004, LESSONS L-002).

- **"Toplama kesintiye uğruyor" iddiası araştırıldı → İDDİA DÜŞTÜ.** Ham toplama hızı
  07-08'den beri sabit (medyan 13 kayıt/gün). %16-17 rakamları 07-21 kalibrasyon
  commit'inden önceki eski metrikten geliyordu. "545 dk kesinti" altyapı arızası
  değil, truncgil'in boş dönmesiydi (Actions o sırada tam zamanında çalışmıştı).
  Gerçek kusur yanıltıcı metrik etiketiydi → `report.classify_gap()`
  (DECISIONS #005, LESSONS L-003).

- **Doküman düzeni + GitHub hazırlığı.** 7 `TESLIMAT*.md` → tek `docs/TESLIMAT-ARSIV.md`
  (kök 18→11 dosya). `.gitignore` yeniden yazıldı — **satır-sonu yorum tuzağı** üç
  gizlilik kuralını sessizce bozmuştu (LESSONS L-005).

- **Şüpheli dosyalar denetlendi.** `scripts/backup.sh` + `backup.ps1` **silindi**:
  işlevini Faz 5'te `data/altin.sql` commit'i devralmıştı, üstelik 2.9 MB binary yedek
  üretip gitignore dışı olduğu için repoya sokuyor ve **onay sormadan push ediyordu**
  (LESSONS L-007). `data/backups/` gitignore'landı. `deploy/`, collector üçlüsü,
  `evds_series.json`, `reports/` gerekçeleriyle **korundu** (LESSONS L-006).

## 2026-07-25 — Backlog'un tamamı kapatıldı (DECISIONS #006)

- **FRED 18 gündür ölü.** Üretimde 07-07'den beri **hiç** çalışmamış (tüm raporlarda
  "veri yok"); yerelden de doğrulandı (iki endpoint, iki User-Agent → hepsi timeout).
  → DXY'ye yfinance `DX-Y.NYB` yedeği (config'de tanımlıydı ama **hiç çağrılmıyordu**);
  panel **5/7 → 6/7**. Reel faize **bilinçli olarak yedek konulmadı** (`^TNX` nominaldir,
  TIPS reel değil). FRED beklemesi 360s → 66s.

- **Z-skor kuru provası kuruldu.** `signals.zscore_dry_run()` → `data/zskor_prova.jsonl`,
  günde 1 satır, **bildirim göndermez**. İlk günden değerli bir ölçüm verdi: kapı GÜN
  sayıyor ama z hâlâ TÜM KAYITLAR üzerinden hesaplanıyor. Kayıt tabanı z=+0.92 (std
  0.118), gün tabanı z=+1.36 (std 0.081) — gün tabanı **daha sık tetikler**.

- **Çeyrek primi kuralı canlandırıldı.** Eşik ve kural vardı, veri vardı (238 kayıt),
  ama `build_context` hep `None` döndürüyordu → var sanılan alarm hiç ateşlenemiyordu.
  Prim z ile aynı kapıya bağlandı. Sezon düzeltmesi yok (gerekçesi yazılı).

- **İkiz eşik mantığı tek kaynağa indirildi.** Ayrışma **zaten oluşmuştu**: üretim
  (`notify`) 5 kural uygularken CLI (`signals`) yalnız 3'ünü biliyordu.

- **Yarım bağlama yakalandı ve kapatıldı.** `quarter_z` yalnız alarm yoluna
  bağlanmıştı; rapor göstermiyor, prova ölçmüyordu → kapı açılınca tetikleyen sayı
  raporda görünmeyecekti. İkisi de bağlandı.

**Gün sonu:** 171 test geçiyor (153 → +18).

---

## 2026-07-27 oturumundan önce tamamlananlar (STATE'ten taşındı)

- İnşa dönemi Faz 1-7 bitti, sistem canlı (`docs/TESLIMAT-ARSIV.md`)
- 2026-07-24 — Çoklu-IDE Usta sistemi kuruldu · kaynak-retry · outbox arşivi ·
  Telegram denetimi (kural ihlali yok) · **history_daily 17 gündür donuktu, düzeltildi**
- 2026-07-24 — Doküman düzeni, `.gitignore` onarımı, ölü script temizliği
- 2026-07-25 — **Backlog'un tamamı kapatıldı** (DECISIONS #006): FRED yedeği,
  z-skor kuru provası, çeyrek primi kuralı, tek kaynak eşik. **171 test.**
- 2026-07-26 — **Karar motoru Faz A+B** (DECISIONS #007): `src/gram.py` (gram
  hakemi + faz-düzeltmeli taban), `src/karar.py` (iki kollu hüküm + sert kapı),
  rapor başına HÜKÜM bloğu, `/hukum` komutu, `config.yaml karar:` bölümü.
  En riskli varsayım ("trade ederek gram artırılabilir") ölçüldü ve **düştü**.
- 2026-07-26 — **Karar motoru Faz C**: `src/tahmin.py` (kayıt/giriş/çözüm/karne),
  3 yeni tablo + `trg_predictions_immutable`, `daily_job` adım 3d, `/karne`
  komutu, HÜKÜM bloğunda karne satırı. **Canlı örneklem-dışı kayıt BAŞLADI** —
  ilk çözüm ~2026-07-31 (1 hafta), ~2026-08-22 (1 ay).
- 2026-07-26 — **Karar motoru Faz D**: `src/ozellikler.py` tek giriş noktası
  (41 özellik); `tahmin.kaydet` + `karar.build_karar` ikisi de buraya bağlandı.
  Look-ahead artık yapısal olarak imkânsız; test sızıntı enjekte edilerek
  düşebildiği doğrulandı. EVDS yayın gecikmesi bir kontaminasyonu kapattı
  (reel net mevduat %12.7 → %13.1).
- 2026-07-26 — **Karar motoru Faz E**: `src/tahmin_backfill.py` aday taraması
  (karne DEĞİL — örneklem-içi üst sınır). 458 haftalık asof, 14 aday,
  **hiçbiri +3.18p eşiğini geçemedi.** Taktik kol kapalı kalıyor — sonuç bu.
  `reports/gram_aday_taramasi.md`.
- 2026-07-26 — **Karar motoru Faz H**: `src/grafik_ciz.py` görsel grafik
  (4 panel PNG), `send_photo`, `/grafik` artık görsel + metin, günlük rapora
  eklendi. matplotlib `requirements.txt`'e girdi ama **lazy import** — yoksa
  sessizce atlanır (`archive.yml` kurmuyor). `data/grafik.png` gitignore'da.
  **Planlanan tüm kodlama fazları bitti.**

---

## 2026-07-27 → 07-29 oturumları (STATE'ten taşındı 2026-08-11)

- **2026-07-27 — Uçtan uca denetim + PUSH** (`7aa3a18`, detay **ADR #008**).
  Push ÖNCESİ denetlendi: karne hiçbir şey ölçmüyordu (yapısal 0.00, L-010),
  `asof=T−1` kâğıt üstündeydi (L-011), `daily_job` hataları yutuyordu, hayalet
  hafta sonu barları kalıcıydı. Hepsi düzeltildi; re-audit 3 eksik daha buldu.
  Devir paketi tazelendi (4 ders + ADR #003).
- **2026-07-27 — Regresyon zırhı + 4 açık kapatıldı** (detay **ADR #009**).
  Gerekçe: abonelik bitince proje daha zayıf modellerle sürecek; kurallar
  taşınabilirdi ama **uygulandığını hiçbir şey doğrulamıyordu**. 299 → **800+
  test** (16 yeni dosya); ağırlık merkezi birim değil **sözleşme**. Testin
  kendisi 20 mutasyonla ölçüldü, 20/20 yakalandı (→ L-015). Kapatılan açıklar:
  `ticks` tekilliği (→ L-013), tahmin kaydının silinebilmesi + `kaynak`/
  `model_version` korumasızlığı (→ L-014), eksik `scripts/backup.sh`.
  `data/altin.sql` 33 699 → **19 369 satır** (sabit nokta doğrulandı).
- **2026-07-29 — Uçtan uca denetim + grafik ölçümü düzeltildi** (detay **ADR #010**).
  Mert: "ne dönüyor anlamıyorum, grafik tahminini kontrol et." Ölçülenler:
  (a) 07-27/28 canlı koşumları 5/5 DoD geçti; (b) retry iddiası **düştü**
  (%6.93 → %6.67, kök sebep truncgil'in tam kesintisi); (c) **faz artefaktı
  düzeltildi** → grafik "zayıf kanıt" 10 → 1; (d) aday taraması **çekirdek**
  eşiğini de raporluyor → açık kolun kuralı eşiğin ALTINDA çıktı; (e) Telegram
  komutlarının üretimde ölü olduğu `telegram_chat.json` ile kanıtlandı.
  Test 797 → **815**; 8 mutasyon, biri ilk turda kaçtı (→ L-016), test
  düzeltildikten sonra 8/8 yakalandı.
